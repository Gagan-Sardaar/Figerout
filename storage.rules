
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write for any authenticated user.
      // You can restrict this further based on your app's needs.
      allow read, write: if request.auth != null;
    }
  }
}
